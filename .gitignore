# Documentation kept local. This repository is public; the design reasoning, the
# build tickets, the earlier drafts and the bring-up instructions are not. Prose
# in the tracked READMEs cites them by name — those are references to documents
# you hold, not links a clone can follow.
mycelium-baseline.md
setup-instr.md
infra/README.md
tickets/
future_work/
CLAUDE.md

gitea-secret

mycelium-keys
mycelium-keys.*

plan.json

node_modules/
dist/
*.tsbuildinfo
.env
.env.*
!.env.example
.DS_Store

# Stray worktree checkouts left behind by agent sessions. Embedded git
# repositories, so committing them adds gitlinks nobody can clone.
.claude/worktrees/
