{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mycelium.local/schemas/event.schema.json",
  "title": "EventEnvelope",
  "description": "One append-only observability record (goal G4). The envelope is fixed and shared by the orchestrator, supervisor, and plan agent; the payload is free-form in v1. Typed per-type payloads are deliberately deferred.",
  "type": "object",
  "additionalProperties": false,
  "required": ["event_id", "ts", "source", "stream_id", "seq", "type"],
  "properties": {
    "event_id": {
      "type": "string",
      "format": "uuid",
      "description": "UUIDv7, minted by the emitter. The idempotency key: replay after an orchestrator outage must not duplicate rows."
    },
    "ts": {
      "type": "string",
      "format": "date-time",
      "description": "Emitter wall clock, UTC. Ordering within a stream comes from seq, not this."
    },
    "source": {
      "enum": ["orchestrator", "supervisor", "agent"],
      "description": "Which tier emitted the event."
    },
    "stream_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "description": "Identifies the emitting process (supervisor id, or plan-agent id). seq is monotonic within one stream_id, which is what makes buffered disk replay orderable."
    },
    "seq": {
      "type": "integer",
      "minimum": 0,
      "description": "Monotonic per stream_id. Gaps mean lost events; duplicates are dropped on event_id."
    },
    "type": {
      "enum": [
        "plan.state_changed",
        "task.state_changed",
        "task.dispatched",
        "task.lease_expired",
        "agent.model_call",
        "agent.tool_call",
        "sandbox.launched",
        "sandbox.exited",
        "limit.exceeded",
        "supervisor.heartbeat",
        "operator.action",
        "egress.allowed",
        "egress.denied",
        "environment.state_changed",
        "error"
      ]
    },
    "severity": {
      "enum": ["debug", "info", "warn", "error"],
      "default": "info"
    },
    "project_id": { "type": ["string", "null"], "format": "uuid", "default": null },
    "plan_id": { "type": ["string", "null"], "format": "uuid", "default": null },
    "task_id": { "type": ["string", "null"], "format": "uuid", "default": null },
    "payload": {
      "type": "object",
      "default": {},
      "description": "Type-specific detail. Untyped in v1 by design: three packages need to agree on the envelope now, and locking payload shapes before the agent loop exists would be guesswork."
    }
  }
}
